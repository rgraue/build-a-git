[project]
name = "build-a-git-core"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn>=0.52.1",
]

[dependency-groups]
dev = [
    "black>=26.5.1",
    "pylint>=4.0.6",
    "pytest>=9.1.1",
]
